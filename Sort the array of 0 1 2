 void sortarray(int a[],int n){
    int l=0,m=0,h=n-1;
    while(m<=h){
     if(a[m]==0){
       swap(a[l],a[m]);
       l++,m++;
      }
    if(a[m]==1) m++;
    if(a[m]==2){
      swap(a[m],a[h]);
        h--;
     }
   }
  }
